import 'package:flutter/material.dart';

class AddEventScreenBody extends StatelessWidget {
  const AddEventScreenBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is the addEventScreen body'),
    );
  }
}
